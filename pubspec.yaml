name: behavioural_22mia1162_hackathon
description: "Detecting Financial Impulse Behaviour in Young Adults"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # ML
  tflite_flutter: ^0.10.4
  
  # Charts & Visualization
  fl_chart: ^0.69.0
  
  # Animations
  lottie: ^3.1.2
  animate_do: ^3.3.4
  
  # UI
  google_fonts: ^6.2.1
  curved_navigation_bar: ^1.0.6
  percent_indicator: ^4.2.3
  flutter_card_swiper: ^7.0.1
  
  # Utils
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  uuid: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/models/